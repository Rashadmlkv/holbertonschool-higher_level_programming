#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    matrixclone = []


    print(len(matrix))
    
    for i in range(len(matrix)):
        j = 0
        while j < len(matrix[i]):
            matrixclone.append(matrix[i][j]**2)
            j += 1
    return matrixclone
